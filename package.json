{
    "name": "personal-site",
    "version": "0.0.0",
    "main": "index.html",
    "scripts": {
        "start": "parcel index.html",
        "build": "rm -rf dist/ && parcel build *.html"
    },
    "dependencies": {
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.3"
    }
}
